{
  "name": "payhero-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "PayHero Kenya monorepo with SDK and Next.js adapter",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "standard": "^17.1.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
